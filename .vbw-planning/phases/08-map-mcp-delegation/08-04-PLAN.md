---
phase: 8
plan: 4
title: "Semantic MCP Capability Detection via Tool Descriptions"
type: execute
wave: 2
depends_on: [1]
cross_phase_deps: []
autonomous: true
effort_override: thorough
skills_used: []
files_modified:
  - commands/map.md
  - tests/map-mcp-delegation.bats
forbidden_commands: []
must_haves:
  truths:
    - "Step 1.3 performs two-pass detection: Pass 1 matches tool name suffixes (existing), Pass 2 matches tool description keywords for unmatched tools"
    - "Description-based matching uses semantic keyword sets per capability category, not string equality on full descriptions"
    - "Name-suffix match always takes priority — description matching only applies to tools not already classified by name"
    - "New tool name suffixes added to cover jcodemunch and jdocmunch tool surfaces"
    - "New capability categories added: CAPABILITY_OUTLINE, CAPABILITY_IMPACT_ANALYSIS, CAPABILITY_CLASS_HIERARCHY, CAPABILITY_DOC_SEARCH, CAPABILITY_DOC_STRUCTURE"
    - "When no code-analysis MCP tools are available, map.md execution is identical to the pre-change behavior"
    - "Tests validate both name-suffix and description-based detection patterns"
  artifacts:
    - path: "commands/map.md"
      provides: "Extended capability detection with description matching"
      contains: "Pass 2"
    - path: "commands/map.md"
      provides: "New capability categories"
      contains: "CAPABILITY_OUTLINE"
    - path: "tests/map-mcp-delegation.bats"
      provides: "Tests for extended detection"
      contains: "description-based"
  key_links:
    - from: "commands/map.md Step 1.3 Pass 1"
      to: "commands/map.md Step 1.3 Pass 2"
      via: "unmatched tools list"
    - from: "commands/map.md Step 1.3"
      to: "commands/map.md Step 3-duo/quad/solo"
      via: "MCP_MAP_CAPABILITIES set (unchanged contract)"
---
<objective>
Extend Step 1.3 in commands/map.md to detect MCP code-analysis capabilities beyond exact tool name suffix matching. Currently, the detection only matches 8 capability categories by tool name endings, which misses servers like jcodemunch (13 tools, only 1 detected) and jdocmunch (13 tools, 0 detected). This plan:

1. Adds new tool name suffixes to existing categories to cover known tools from jcodemunch, jdocmunch, and similar servers
2. Adds new capability categories for tool types not currently recognized (outlines, impact analysis, class hierarchy, doc search, doc structure)
3. Introduces a second detection pass that examines tool descriptions (the `description` field MCP servers expose) using semantic keyword matching, catching tools whose names don't match any suffix pattern
4. Updates downstream capability-to-document routing in duo/quad/solo to leverage the new categories
5. Adds tests covering the extended detection patterns
</objective>
<context>
@commands/map.md -- Current map command with Step 1.3 capability detection
@.vbw-planning/phases/08-map-mcp-delegation/08-01-PLAN.md -- Original detection plan (patterns and structure)
@tests/map-mcp-delegation.bats -- Existing tests to extend

## Gap Analysis (jcodemunch tools → current patterns)

| jcodemunch Tool | Current Match | Gap |
|---|---|---|
| search_symbols | CAPABILITY_SYMBOL_SEARCH ✓ | -- |
| get_symbol_source | -- | suffix `get_symbol_source` not in CAPABILITY_CODE_SNIPPET |
| get_file_outline | -- | no OUTLINE capability category |
| get_repo_outline | -- | no OUTLINE capability category |
| get_ranked_context | -- | entirely new (context assembly) |
| find_importers | -- | suffix not in CAPABILITY_CALL_TRACING |
| get_blast_radius | -- | suffix not in CAPABILITY_HOTSPOT_ANALYSIS |
| get_class_hierarchy | -- | no CLASS_HIERARCHY capability category |
| find_dead_code | -- | no DEAD_CODE capability (maps to CONCERNS.md) |
| get_changed_symbols | -- | suffix not in CAPABILITY_HOTSPOT_ANALYSIS |
| get_symbol_importance | -- | no IMPORTANCE capability (maps to ARCHITECTURE.md) |
| get_layer_violations | -- | architectural (maps to CONCERNS.md) |
| audit_agent_config | -- | operational, not mapping-relevant |

## Gap Analysis (jdocmunch tools → current patterns)

| jdocmunch Tool | Current Match | Gap |
|---|---|---|
| index_local | -- | suffix not in CAPABILITY_INDEX |
| index_repo | -- | suffix not in CAPABILITY_INDEX |
| list_repos | -- | operational, not mapping-relevant |
| get_toc / get_toc_tree | -- | no DOC_STRUCTURE capability |
| get_document_outline | -- | no DOC_STRUCTURE capability |
| search_sections | -- | no DOC_SEARCH capability |
| get_section / get_sections | -- | no DOC_SEARCH capability |
| get_section_context | -- | no DOC_SEARCH capability |
| delete_index | -- | operational, not mapping-relevant |
| get_broken_links | -- | quality (maps to CONCERNS.md) |
| get_doc_coverage | -- | cross-ref (maps to CONCERNS.md) |
</context>
<tasks>
<task type="auto">
  <name>Extend name-suffix patterns for existing capability categories</name>
  <files>
    commands/map.md
  </files>
  <action>
In Step 1.3, extend the existing 8 capability pattern definitions with additional tool name suffixes drawn from jcodemunch, jdocmunch, and other known MCP servers. Keep all existing suffixes — only append new ones.

Updated patterns:

```
CAPABILITY_ARCHITECTURE    := (existing) + get_repo_outline, get_symbol_importance
CAPABILITY_SYMBOL_SEARCH   := (existing) + search_symbols (already present — no change needed)
CAPABILITY_DEPENDENCY_GRAPH := (existing) + find_importers, get_layer_violations
CAPABILITY_CALL_TRACING    := (existing) + find_importers, get_blast_radius
CAPABILITY_CODE_SEARCH     := (existing) + search_sections, get_section, get_sections
CAPABILITY_CODE_SNIPPET    := (existing) + get_symbol_source, get_section_context
CAPABILITY_HOTSPOT_ANALYSIS := (existing) + get_blast_radius, get_changed_symbols, find_dead_code, get_broken_links, get_doc_coverage
CAPABILITY_INDEX           := (existing) + index_local, index_repo, index_folder
```

Note: some tools (e.g., `find_importers`, `get_blast_radius`) map to multiple categories — this is intentional as they provide data useful for multiple mapping documents.
  </action>
  <verify>
Read Step 1.3 in commands/map.md. Confirm each of the 8 existing categories has new suffixes appended. Confirm no existing suffixes were removed.
  </verify>
  <done>
All 8 existing capability categories extended with new suffixes covering jcodemunch and jdocmunch tool surfaces.
  </done>
</task>
<task type="auto">
  <name>Add new capability categories for uncovered tool types</name>
  <files>
    commands/map.md
  </files>
  <action>
Add 3 new capability categories to Step 1.3, after the existing 8:

```
CAPABILITY_OUTLINE         := tool name ending in: get_file_outline, get_repo_outline, get_document_outline, get_toc, get_toc_tree
CAPABILITY_IMPACT_ANALYSIS := tool name ending in: get_blast_radius, get_changed_symbols, find_dead_code, get_layer_violations, get_doc_coverage, get_broken_links
CAPABILITY_CLASS_HIERARCHY := tool name ending in: get_class_hierarchy, class_hierarchy, get_inheritance
```

Update the capability-to-document routing in the `<mcp_code_analysis>` blocks (Step 3-duo, Step 3-quad, Step 3-solo) to include the new categories:

```
- Outline extraction → STRUCTURE.md (module layout, file organization)
- Impact analysis → CONCERNS.md (risk areas, dead code, broken links, layer violations)
- Class hierarchy → ARCHITECTURE.md (inheritance chains, type relationships)
```

Also update the per-Scout routing in quad mode:
- Scout 1 (Tech Stack): add CAPABILITY_OUTLINE for STRUCTURE.md context
- Scout 2 (Architecture): add CAPABILITY_CLASS_HIERARCHY
- Scout 4 (Concerns): add CAPABILITY_IMPACT_ANALYSIS
  </action>
  <verify>
Read Step 1.3 and confirm 11 total capability categories (8 original + 3 new). Read Step 3-duo, Step 3-quad, Step 3-solo and confirm the new categories appear in capability-to-document routing.
  </verify>
  <done>
Three new capability categories added with document routing in all three mode branches.
  </done>
</task>
<task type="auto">
  <name>Add Pass 2: description-based semantic matching</name>
  <files>
    commands/map.md
  </files>
  <action>
After the existing name-suffix matching in Step 1.3 (now "Pass 1"), add a "Pass 2: Description-based matching" section. This pass examines tool descriptions for tools that were NOT already classified by Pass 1.

Structure:

```
**Pass 2: Description-based matching (unclassified tools only)**

For each MCP tool not matched by Pass 1, use `ToolSearch` to load its schema and read its `description` field. Match the description text against these semantic keyword sets (case-insensitive):

CAPABILITY_ARCHITECTURE    := description contains: "architecture", "structure overview", "entry point", "package breakdown", "codebase overview"
CAPABILITY_SYMBOL_SEARCH   := description contains: "find symbol", "search symbol", "locate function", "locate class", "symbol lookup", "find definition"
CAPABILITY_DEPENDENCY_GRAPH := description contains: "dependency", "import graph", "module relationship", "dependency tree"
CAPABILITY_CALL_TRACING    := description contains: "call graph", "caller", "callee", "call chain", "call hierarchy", "who calls", "what calls"
CAPABILITY_CODE_SEARCH     := description contains: "search code", "text search", "content search", "search source", "search section"
CAPABILITY_CODE_SNIPPET    := description contains: "source code for", "retrieve function", "retrieve symbol", "get source", "extract code", "symbol source"
CAPABILITY_HOTSPOT_ANALYSIS := description contains: "complexity", "hotspot", "change impact", "risk score", "blast radius", "dead code", "unreachable"
CAPABILITY_INDEX           := description contains: "index repository", "index folder", "index codebase", "reindex", "build index", "index local"
CAPABILITY_OUTLINE         := description contains: "outline", "table of contents", "file structure", "symbol list", "section hierarchy"
CAPABILITY_IMPACT_ANALYSIS := description contains: "impact", "blast radius", "breaking change", "dead code", "coverage gap", "broken link", "layer violation"
CAPABILITY_CLASS_HIERARCHY := description contains: "inheritance", "class hierarchy", "type hierarchy", "class relationship", "subclass", "superclass"

Rules:
1. Pass 1 (name suffix) always takes priority — Pass 2 only runs on tools not already classified
2. A tool can match at most one category in Pass 2 (first match wins, categories checked in the order listed above)
3. If a tool's description matches no keyword set, skip it — do not force-classify
4. Tools classified by Pass 2 are added to MCP_MAP_CAPABILITIES with the same structure as Pass 1 results
5. Display update: `◆ MCP: {N} code-analysis capabilities detected ({M} by name, {K} by description) — will delegate structural analysis`
```

This ensures that novel MCP servers with unconventional tool names but clear descriptions are still detected, while well-known tools continue to be matched instantly by name.
  </action>
  <verify>
Read Step 1.3 in commands/map.md. Confirm: (1) Pass 1 and Pass 2 are clearly labeled, (2) Pass 2 only processes tools not matched by Pass 1, (3) description keyword sets cover all 11 capability categories, (4) first-match-wins rule is stated, (5) display line shows name vs description breakdown.
  </verify>
  <done>
Pass 2 description-based matching added with 11 keyword sets, priority rules, and updated display format.
  </done>
</task>
<task type="auto">
  <name>Add tests for extended detection patterns</name>
  <files>
    tests/map-mcp-delegation.bats
  </files>
  <action>
Add new test cases to tests/map-mcp-delegation.bats covering:

1. **Extended suffix coverage**: Verify the new suffixes appear in Step 1.3 (get_symbol_source, find_importers, get_blast_radius, get_changed_symbols, index_local, index_repo, search_sections, get_section, get_file_outline, get_repo_outline, get_document_outline, get_toc, get_class_hierarchy, find_dead_code)

2. **New capability categories**: Verify CAPABILITY_OUTLINE, CAPABILITY_IMPACT_ANALYSIS, and CAPABILITY_CLASS_HIERARCHY are defined in Step 1.3

3. **Pass 2 presence**: Verify "Pass 2" or "description-based" text exists in Step 1.3

4. **Description keyword sets**: Verify description keyword sets exist for all 11 categories

5. **Priority rule**: Verify the pass priority rule is stated (name suffix takes priority over description)

6. **Updated routing**: Verify new categories appear in capability-to-document routing in Step 3 sections (CAPABILITY_OUTLINE → STRUCTURE.md, CAPABILITY_IMPACT_ANALYSIS → CONCERNS.md, CAPABILITY_CLASS_HIERARCHY → ARCHITECTURE.md)

7. **No regression**: Verify all 8 original capability categories still have their original suffixes intact
  </action>
  <verify>
Run `bats tests/map-mcp-delegation.bats` and confirm all tests pass (both new and existing).
  </verify>
  <done>
Extended test suite validates new suffixes, new categories, description-based matching, priority rules, and no regression on original patterns.
  </done>
</task>
</tasks>
<verification>
1. Read commands/map.md Step 1.3 — confirm 11 capability categories with extended suffix lists
2. Confirm Pass 1 (name suffix) and Pass 2 (description keywords) are both present and clearly separated
3. Confirm Pass 2 only processes unclassified tools (priority rule stated)
4. Confirm description keyword sets cover all 11 categories
5. Confirm new categories appear in capability-to-document routing in Step 3-duo, Step 3-quad, Step 3-solo
6. Confirm display line includes name vs description breakdown
7. Run bats tests — all pass
8. Verify no hardcoded MCP server names in detection logic
9. Verify no-MCP path is unchanged (empty MCP_MAP_CAPABILITIES → identical behavior)
</verification>
<success_criteria>
- Step 1.3 has two detection passes: name suffix (Pass 1) and description keywords (Pass 2)
- 11 capability categories (8 original extended + 3 new)
- jcodemunch: 12 of 13 tools now detectable (audit_agent_config excluded — operational)
- jdocmunch: 11 of 13 tools now detectable (list_repos, delete_index excluded — operational)
- Name-suffix match always takes priority over description match
- Novel MCP servers with clear tool descriptions are detected without needing suffix updates
- No behavioral change when no code-analysis MCP tools are installed
- All existing tests continue to pass, new tests validate extended detection
</success_criteria>
<output>
04-SUMMARY.md
</output>
